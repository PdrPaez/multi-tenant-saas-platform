name: CI
on: [push, pull_request]
jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: sudo apt-get update && sudo apt-get install -y postgresql-16 postgresql-client-16 && sudo systemctl start postgresql
      - run: sudo -u postgres psql -c "CREATE DATABASE saas_demo;" && sudo -u postgres psql -d saas_demo -f ../scripts/bootstrap-postgres.sql
      - run: pip install -e '.[dev]' && alembic upgrade head && python -m app.seed && ruff check . && pytest && python -m app.evaluation.run
        working-directory: backend
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '20'}
      - run: npm install && npm run lint && npm run build
        working-directory: frontend
