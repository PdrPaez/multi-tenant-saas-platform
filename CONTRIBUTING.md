# Contributing

Branch from `dev` using `feature/MTS-XXX-description`, `fix/`, `test/`, `docs/`, or `chore/`. Every meaningful change maps to an `MTS-XXX` card and commits use `[AREA][MTS-XXX] concise English description`. PostgreSQL integration tests are required for security changes. Do not commit secrets or automated-authorship metadata.

